#include <iostream>
using namespace std;
int main()
{
    // char y = 'c';
    // int x = 10;
    // int *ptr;
    // ptr = &x;

    // int *ptr = &x;
    // char *pointer = &y;
    // wrong int *pinter = &y;

    // cout << ptr << endl;
    // cout << *ptr << endl;

    // int x = 0;

    // int *ptr; 
    // int* ptr;
    // int * ptr; all three ways are correct

    // int *ptr = &x;
    // int **ptr1;
    // ptr1 = &ptr;
    // ptr2 = &x;
    // cout << ptr1 << endl; // address of ptr
    // cout << **ptr1 << endl;

    // int a[] = {2, 3, 4, 5, 6};
    // int *ptr;
    // int *ptr1;
    // ptr = a; // automatic stores addrress of first index
    // wrong --? ptr=&a;
    // ptr1 = &a[0];

    // cout << ptr << endl;
    // cout << ptr1 << endl;
    // return 0;
}